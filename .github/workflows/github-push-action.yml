name: "My first github Actions"
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

    #   - name: Build project
    #     run: npm run build

      - name: Deploy to production
        run: |
          echo "Deploying to production..."
          mkdir -p ~/.ssh
          echo "${{ secrets.HOST_TKVM064 }}" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
          ls -l .
