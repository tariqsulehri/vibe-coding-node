{
  "name": "vibecoding-node-server",
  "version": "1.0.0",
  "description": "Express + Mongoose boilerplate with faker mock route",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js",
    "lint": "eslint . --ext .js",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "helmet": "^6.0.1",
    "mongoose": "^7.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "eslint": "^8.46.0"
  }
}
